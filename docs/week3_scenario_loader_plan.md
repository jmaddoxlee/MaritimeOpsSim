# Week 3 Scenario Loader Plan

## Goal

Build the first version of the scenario loading system for MaritimeOpsSim.

The simulator should load mission information from a JSON file instead of hardcoding mission and vessel data in `main.cpp`.

## Scenario File

Primary demo scenario:

```text
scenarios/search_rescue_demo.json
```
